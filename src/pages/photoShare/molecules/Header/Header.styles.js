import styled from 'styled-components';

export const HeaderContainer = styled.div`
    position: relative;
    z-index: 12;
    margin-bottom: 40px;
    overflow: unset;
    & .scrollMobile{
        overflow: unset;
    }
    & .btn{
        margin: 0 5px !important;
        padding: 0 20px;
    }
`;