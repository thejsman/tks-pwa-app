import Meteor from "react-web-meteor";

export const Chat = Meteor.collection('chat');
export const ChatRequests = Meteor.collection('chatrequests');
export const NotificationCount = Meteor.collection('notificationCount');
